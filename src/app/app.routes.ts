import { Routes } from '@angular/router';
import { BillGatesComponent } from './bill-gates/bill-gates.component';

export const routes: Routes = [
  { path: '', component: BillGatesComponent }  // Подключаем компонент
];
